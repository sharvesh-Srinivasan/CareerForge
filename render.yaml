services:
  - type: web
    name: careerforge-api
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: PORT
        value: 4000
      - key: NODE_ENV
        value: production
